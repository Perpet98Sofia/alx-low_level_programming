#include <stdio.h>>
/**
 * main - prints hexadecimal base 0123456789abcdef, using putchar
 * Return: always 0 (success)
 */

int main(void)
{
char a;
char b;

for (a = 48; a <= 57; n++);
{
putchar (n);
}
for (b = 97; b <= 102; m++);
{
putchar (m);
}
putchar ('\n');
return (0);
}
